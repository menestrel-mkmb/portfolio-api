meta {
  name: All courses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/courses
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  const body = res.getBody(Object);
  console.log(body);
}
